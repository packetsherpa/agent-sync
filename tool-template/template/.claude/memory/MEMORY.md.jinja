# Memory

Durable facts, decisions, and landmines for {{ project_name }}. One file per
fact. Live/churning state lives in {% if include_project_context %}`project-context.md`{% else %}the project docs{% endif %}, not here.

<!-- Add entries as one line each:
     - [Title](slug.md) — one-line hook of what the fact is
     Each entry links to a sibling <slug>.md with frontmatter:
       ---
       name: <slug>
       description: <one line>
       metadata:
         type: user | feedback | project | reference
       ---
-->
